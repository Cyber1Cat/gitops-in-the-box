apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-guestbook-workload
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Cyber1Cat/gitops-in-the-box.git'
    targetRevision: main
    path: apps/guestbook
    helm:
      valueFiles:
        - values-prod.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
